name: Metrics
on:
  # Chạy tự động lúc 00:00 mỗi ngày
  schedule: [{cron: "0 0 * * *"}]
  # Cho phép chạy thủ công
  workflow_dispatch:
  # Chạy mỗi khi push vào nhánh master/main (để test)
  push: {branches: ["master", "main"]}

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          # Token bạn vừa tạo ở Bước 1 & 2
          token: ${{ secrets.METRICS_TOKEN }}

          # Cấu hình chung
          user: hvb1412
          template: classic
          base: header, activity, community, repositories, metadata
          config_timezone: Asia/Ho_Chi_Minh
          
          # 1. Plugin: Isometric Calendar (Thành phố 3D - Cực đẹp) 🏙️
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year
          
          # 2. Plugin: Languages (Ngôn ngữ lập trình) 💻
          plugin_languages: yes
          plugin_languages_ignored: >-
            html, css
          plugin_languages_details: lines, percentage
          plugin_languages_sections: most-used
          plugin_languages_indepth: yes

          # 4. Plugin: Achievements (Thành tựu) 🏆
          plugin_achievements: yes
          plugin_achievements_display: compact
          plugin_achievements_threshold: C